let a = Number (prompt('Escribeme un numero', ''));
let b = Number (prompt('Escribeme un numero', ''));
min(a,b);

function min(a, b) { 
    if (a > b) {
        alert ("min("+ a + ", " + b +") == "+ b);
    }

    else if  (a < b) {
        alert ("min("+ a + ", " + b +") == "+a);
    }
   
    else if (a = b)
        alert ("min("+ a + ", " + b +") == "+a);
    }
 



