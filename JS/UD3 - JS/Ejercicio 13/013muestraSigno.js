let numero = Number (prompt('Escribeme un numero', ''));

    if (numero > 0 ) {
        alert( "¡Es 1!" );
    
    }

    else if (numero < 0 ) {
    
        alert( "¡Es -1!" );

    }

    else if (numero == 0 ) {

        alert( "¡Es 0!" );

    }